<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ssm.mapper.UserMapper">

    <resultMap id="UserMap" type="User">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="age" column="age"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="cols">id, name, age, email, phone, create_time, update_time</sql>

    <select id="selectById" resultMap="UserMap">
        SELECT <include refid="cols"/> FROM t_user WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="UserMap">
        SELECT <include refid="cols"/> FROM t_user
        <where>
            <if test="keyword != null and keyword != ''">
                name LIKE CONCAT('%', #{keyword}, '%') OR email LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM t_user
        <where>
            <if test="keyword != null and keyword != ''">
                name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
    </select>

    <insert id="insert" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_user (name, age, email, phone) VALUES (#{name}, #{age}, #{email}, #{phone})
    </insert>

    <update id="update" parameterType="User">
        UPDATE t_user SET name=#{name}, age=#{age}, email=#{email}, phone=#{phone} WHERE id=#{id}
    </update>

    <delete id="deleteById">
        DELETE FROM t_user WHERE id = #{id}
    </delete>

</mapper>
